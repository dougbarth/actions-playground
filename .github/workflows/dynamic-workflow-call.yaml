on:
  workflow_dispatch:
    inputs:
      workflow_name:
        required: false

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello

      - uses: ./.github/workflows/callable-workflow.yaml